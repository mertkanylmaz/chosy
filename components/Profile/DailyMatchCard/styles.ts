/**
 * DailyMatchCard stilleri — bileşen dosyasında StyleSheet.create içinde tanımlandı.
 * Bu dosya mimari kuralı gereği oluşturulmuştur (her component: index.tsx + styles.ts).
 *
 * Stilller bileşen dosyasında yönetilmektedir çünkü archetype rengi
 * runtime'da hesaplanan dinamik bir değerdir.
 */

export {};
