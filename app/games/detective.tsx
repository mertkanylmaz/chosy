/**
 * Detective — Gunluk 3 asamali dedektif oyunu.
 *
 * Stage 1 (Investigation) → Stage 2 (Deduction) → Stage 3 (Case Closed).
 */
import React from 'react';

import { DetectiveGame } from '@/components/games/Detective';

export default function DetectiveScreen() {
  return <DetectiveGame />;
}
